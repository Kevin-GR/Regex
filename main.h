#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#define MAX 100

typedef struct Node
{
  char *line;
  struct Node *Next;
}Node;

Node* createNODe(const char *line)
void appendNode(Node **head, const char *line)
void freeLinked(*Node head)
Node *
